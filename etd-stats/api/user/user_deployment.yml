apiVersion: apps/v1
kind: Deployment
metadata:
  name: etdstats-user
  namespace: etd
spec:
  replicas: 2
  selector:
    matchLabels:
      app: etdstats-user
  template:
    metadata:
      labels:
        app: etdstats-user
    spec:
      containers:
        - name: etdstats-user
          image: 'ghcr.io/etherdata-blockchain/etdstats/user_service:1.6.0'
          resources:
            requests:
              memory: "100Mi"
              cpu: "100m"
            limits:
              memory: "300Mi"
              cpu: "200m"
          ports:
            - name: server-port
              containerPort: 8080
              protocol: TCP
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: etdstats
                  key: DATABASE_URL
            - name: REDIS_URL
              valueFrom:
                secretKeyRef:
                  key: REDIS_URL
                  name: etdstats
      restartPolicy: Always
